# yzv405Project-iNFY

Idiom-based image ranking pipeline for AdMIRe submissions.

## Setup

1. Install dependencies:
   `pip install -r requirements.txt`
2. Set API key in PowerShell:
   `$env:OPENROUTER_API_KEY='your_api_key_here'`
3. Run:
   `python run_all.py`
